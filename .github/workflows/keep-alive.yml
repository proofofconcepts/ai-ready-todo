name: Keep Alive

on:
  schedule:
    - cron: '*/10 * * * *' # a cada 10 minutos

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: |
          curl --fail ${{ secrets.RENDER_BACKEND_URL }}/health
